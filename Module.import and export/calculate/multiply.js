function calculatemultiply(a, b) {
  const result = a * b;
  console.log(result);
}

module.exports = { calculatemultiply }; //we are importing it to app.js.

// always import and export with object whether it is one thing or more than one thing.
